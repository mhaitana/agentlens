/**
 * @agentlens/recommendations — versioned rule engine that evaluates persisted
 * metrics against rules and emits {@link Recommendation} records (spec §7,
 * §13.2). Rules carry a `ruleId` + `ruleVersion` so guidance can be tracked
 * and retracted.
 *
 * Skeleton — implementation lands in feature F004.
 */
export const RECOMMENDATIONS_VERSION = "0.0.0";
