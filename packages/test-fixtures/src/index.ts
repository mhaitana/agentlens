/**
 * @agentlens/test-fixtures — shared, deterministic fixtures (synthetic
 * transcripts, hook payloads, OTLP spans, redaction golden cases) used by
 * packages and apps (spec §12). Kept dependency-free so any package can pull
 * it in test scope without dragging runtime deps.
 *
 * Skeleton — implementation lands alongside the features that consume it.
 */
export const TEST_FIXTURES_VERSION = "0.0.0";
