/**
 * @agentlens/otel-receiver — receives OpenTelemetry (OTLP/HTTP) spans emitted
 * by Claude Code's telemetry and normalises them into NormalisedSourceEvent
 * (spec §4.5, §13.1).
 *
 * Skeleton — implementation lands in feature F003.
 */
export const OTEL_RECEIVER_VERSION = "0.0.0";
