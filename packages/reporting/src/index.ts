/**
 * @agentlens/reporting — renders persisted metrics into terminal tables,
 * Markdown reports, and JSON exports; supports date-range + project filters
 * (spec §11, §13.4).
 *
 * Skeleton — implementation lands in feature F006.
 */
export const REPORTING_VERSION = "0.0.0";
