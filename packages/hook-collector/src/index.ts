/**
 * @agentlens/hook-collector — ingests Claude Code hook events (PreToolUse,
 * PostToolUse, Stop, …) via a local HTTP endpoint and persists them through
 * the redaction-before-persistence pipeline (spec §4.4, §13.1).
 *
 * Skeleton — implementation lands in feature F003.
 */
export const HOOK_COLLECTOR_VERSION = "0.0.0";
