/**
 * @agentlens/prompt-coach — analyses prompt patterns (anti-pattern detection,
 * clarity, specificity, context management) and produces coaching feedback
 * (spec §8, §13.2).
 *
 * Skeleton — implementation lands in feature F005.
 */
export const PROMPT_COACH_VERSION = "0.0.0";
